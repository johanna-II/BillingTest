# ⚙️ 환경 변수 설정 가이드
#
# 로컬 개발용 설정 파일 생성:
# 1. 이 파일을 .env.local로 복사: cp env.example.txt .env.local
# 2. 필요한 값을 설정
# 3. npm run dev 재시작

# ===========================================
# API Configuration (REQUIRED)
# ===========================================

# Cloudflare Workers Backend (권장)
NEXT_PUBLIC_API_URL=https://billing-api.janetheglory.workers.dev

# 로컬 Python Mock Server (개발 테스트용)
# NEXT_PUBLIC_API_URL=http://localhost:5000

# 프로덕션 배포 시:
# - Vercel: Dashboard → Settings → Environment Variables 에서 설정
# - GitHub Actions: Repository Secrets 에서 설정
# - ⚠️ vercel.json이나 GitHub workflow 파일에 직접 쓰지 마세요!

# ===========================================
# Optional: Feature Flags
# ===========================================
# NEXT_PUBLIC_ENABLE_PDF_EXPORT=true
# NEXT_PUBLIC_ENABLE_COMPARISON=true
# NEXT_PUBLIC_ENABLE_HISTORY=true

# ===========================================
# Optional: Analytics (선택사항)
# ===========================================
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxxx

# ===========================================
# Optional: API Keys (선택사항)
# ===========================================
# API_KEY=your-secret-api-key
# BILLING_API_TOKEN=your-billing-token
